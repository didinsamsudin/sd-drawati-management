{
    "name": "manajemen-sd-drawati-monorepo",
    "version": "1.0.0",
    "private": true,
    "scripts": {
        "postinstall": "cd client && npm install && cd ../server && npm install",
        "build": "cd client && npm run build"
    },
    "workspaces": [
        "client",
        "server"
    ]
}